import React from 'react';
import {
    Button,
} from './RoundButton.styled';

export const RoundButton = ({text}) => {
    return (
        <>
            <Button>{text}</Button>
        </>
    )
}